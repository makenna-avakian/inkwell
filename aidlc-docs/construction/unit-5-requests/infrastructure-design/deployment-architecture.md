# Deployment Architecture — Unit 5: Commission Requests & Messaging

Identical to Units 1-4's deployment (same app, same Neon database, same CI/CD). No new diagram — only a new migration and object key path.

## Rollback Path
Code-only rollback remains sufficient (no destructive migrations).
